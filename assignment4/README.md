Task 1 --> (task1.py)
Opens and reads a text file named sample.txt and prints its content line by line.
It handles errors gracefully if the file does not exist without the program crashing.

task 2-->  (task2.py)
 Takes user input and writes it to a file named output.txt and appends additional data to the same file.
it reads and displays the final content of the file.

